[project]
name = "hermes-agent"
version = "0.1.0"
description = "AI Agent Framework"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "playwright>=1.61.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
